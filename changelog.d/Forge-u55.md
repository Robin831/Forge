category: Fixed
- **Retry action for exhausted PRs now works reliably** - Fixed multiple issues preventing the Retry action on exhausted PRs (e.g., "Rebase exhausted 3/3") from re-triggering Bellows monitoring. The fix addresses: clearing the Bellows snapshot cache so transitions are re-detected, resetting the lifecycle manager's in-memory state so new events aren't silently dropped as exhausted, a race condition in snapshot reads during concurrent reset/poll, and ensuring merge conflicts correctly transition PR status to "needs fix". Added comprehensive test coverage for the full retry flow. (Forge-u55)
