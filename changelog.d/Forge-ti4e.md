category: Fixed
- **Self-deploy drain ignores bellows monitor rows** - The drain guard now waits only on workers that own a live process or pipeline — smith, the quench/burnish/rebase/assay fix workers, a pipeline still finalizing after review, and operator-paused workers. Bellows' per-PR monitor rows are non-terminal but hold no PID, worktree or goroutine and live as long as their PR stays open, so counting them made a single PR parked in the fix loop defer every self-deploy for the full `max_drain_wait`. (Forge-ti4e)
