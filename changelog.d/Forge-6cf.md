category: Added
- **Per-anvil depcheck toggle** - Added `depcheck_enabled` per-anvil setting to disable automatic dependency update scanning for specific anvils. When set to `false`, the depcheck monitor skips that anvil entirely. Useful for repos that handle dependency updates via their own bead formulas. (Forge-6cf)
