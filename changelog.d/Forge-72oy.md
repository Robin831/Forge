category: Fixed
- **Assay `files=` telemetry and retry diff scoping** - The Assay pass tracker recorded a file only from a `file_path`-shaped tool input, but measured over 95 pass sessions every one of 742 tool calls was `Bash` — so `files=` was 0 on every logged pass and the turn-budget retry's third modification (the diff scoped to the files the failed session opened) was never constructed. Paths are now also read from shell command lines and from the `path`/`notebook_path` input keys; only files already in the diff can be selected, so a misparsed argument can never widen a retry's scope. (Forge-72oy)
