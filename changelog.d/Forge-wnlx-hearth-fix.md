category: Fixed
- **Force Smith note input now appears in Hearth** - Selecting "Force Smith" in the Needs Attention action menu now correctly shows the note input form instead of doing nothing. Two bugs fixed: missing `Init()` call (form never started) and missing `View()` render (form ran invisibly). (Forge-wnlx)
