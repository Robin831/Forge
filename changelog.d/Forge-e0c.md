category: Fixed

- **Hearth Needs Attention scroll** - Fixed viewport scrolling in the Needs Attention panel where items at the top would disappear when scrolling down, matching the viewport offset pattern already used in the Workers panel (Forge-e0c)
