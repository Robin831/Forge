category: Added
- **Bead detail Dependencies panel + dep modal** - The `/bead/:id` page now shows a Dependencies panel with two columns (Blocks / Blocked by). Clicking a dep opens a modal that loads the bead's brief and immediate deps so users can walk the graph without leaving the page; an "Open full page" link routes to the dedicated bead view. (Forge-9ygp)
