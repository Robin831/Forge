category: Fixed
- **Kill and steer a stalled worker from the dashboard** - A stalled worker's panel offered neither control even though the daemon accepts both: `killWorkerProcess` applies no status gate at all, and the steer path gates on a live pipeline control handle rather than on the DB status the watchdog masks. The kill button now appears for `stalled`, and the steer composer is enabled for it — in the worker panel, the workers pane, the bead detail page and the expanded log view alike — instead of claiming "No active pipeline". Both gates are single shared predicates (`canKillWorker`, `isSteerTargetStatus`) that every one of those surfaces now reads, rather than the inline copies that let the status be missed in several places at once. (Forge-ov1u)
