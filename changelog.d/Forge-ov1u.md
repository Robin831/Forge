category: Fixed
- **Kill and steer a stalled worker from the dashboard** - A stalled worker's panel offered neither control even though the daemon accepts both: `killWorkerProcess` applies no status gate at all, and the steer path gates on a live pipeline control handle rather than on the DB status the watchdog masks. The kill button now appears for `stalled`, the steer composer is enabled for it instead of claiming "No active pipeline", and both gates are single shared predicates (`canKillWorker`, `isSteerTargetStatus`) rather than the four inline copies that let the status be missed everywhere at once. (Forge-ov1u)
