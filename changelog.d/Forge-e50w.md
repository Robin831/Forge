category: Changed
- **Hearth Workers panel now uses Bubbles table component** - Replaced the hand-rolled worker list rendering with the `charmbracelet/bubbles/table` component for better column management, header styling, and cursor navigation. Added worker ID following to keep the selection stable during list updates. (Forge-e50w)
