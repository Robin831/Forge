category: Changed
- **Hearth Workers panel now uses Bubbles table component** - Replaced the hand-rolled worker list rendering with the `charmbracelet/bubbles/table` component for better column management, header styling, and cursor navigation. (Forge-e50w)
