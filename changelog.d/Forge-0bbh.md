category: Changed
- **Assay sessions grouped into their run in the bead Logs panel** - An Assay review writes one log per model session — triage plus five deep passes — and the panel rendered each as its own row labelled "assay", so one review read as six runs and therefore as six times the spend. Sessions are now named `assay-<run>-<pass>-<ts>-<seq>.log` and fold into a single collapsible row carrying the run's totals ("5/5 passes, 1 finding, $8.75, 214s"), each session identified by its pass and marked with the findings it contributed. A re-reviewed PR shows one row per run in time order, and logs written before the change still list, labelled "assay". (Forge-0bbh)
