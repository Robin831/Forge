category: Fixed
- **Live worker panels follow the worker instead of freezing on the Smith log** - A panel's log stream now notices when the worker's log file moves to its next stage (Smith iterations, the Warden review — which now repoints the worker row to its session log — and burnish fix → verify), emits a divider line, and continues with the new file. Previously the stream tailed whichever file existed at connection time, so panels sat frozen on the finished Smith transcript until a page refresh. (Forge-hyla)
- **Finished worker panels linger for three minutes** - `/api/workers` accepts `?recent=<seconds>` to include recently-finished workers, and the dashboard renders them as dimmed frozen panels with the final transcript and a "done · Xm ago" caption instead of unmounting them on the next 5s poll. Finished panels never consume idle Smith slots, and the TUI (which sends no payload) is unchanged. (Forge-hyla)
