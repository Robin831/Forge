category: Added
- **Native Linux packages (deb, rpm, apk) via GoReleaser nfpms** - Releases now produce deb, rpm, and apk packages enabling installation via `apt install forge` or `yum install forge`. Includes a systemd service file for daemon autostart on Linux. (Forge-x0rf)
