category: Added
- **Assay stage_providers keys** - `stage_providers` (global and per-anvil) now accepts `assay` and the per-pass keys `assay.triage`, `assay.logic`, `assay.security`, `assay.conventions`, `assay.tests-missing` and `assay.repo-specific`. The Hearth settings page lists the pass keys under a collapsed "Assay passes" disclosure, rejects unknown pass names and empty chains, and a test keeps the frontend stage list in sync with the backend. `stage_providers` is now read from the raw YAML, since viper split the dotted keys and made the config fail to load; edits hot-reload without a restart. (Forge-8x3wp)
