## Install

```powershell
irm https://raw.githubusercontent.com/Robin831/Forge/main/install.ps1 | iex
```

Or download the archive for your platform below.

## Features

- Auto-learn skip events in Hearth event log (Forge-7spt)
- One-liner install script for Linux and macOS (Forge-t3ba)
- Spinner animations for active workers and crucible phases (Forge-3gjk)
- `max_pipeline_iterations` config setting (Forge-ga7l)

## Bug Fixes

- Bellows reliably retries CI fixes after failed attempts (Forge-bzk6)
- Crucible child failure no longer causes orphan recovery loop (Forge-flf0)
- Crucible child failures now correctly prevent standalone re-dispatch (Forge-roki)
- Decomposed child beads now inherit the parent's auto_dispatch tag (Forge-fk5f)
- Decomposed flag no longer clears retry record when no children created (Forge-0qj7)
- Failed pipeline now clears bead assignee on release (Forge-3kdt)
- Orphan recovery no longer resets active Crucible parent beads (Forge-epfe)
- PR title now reflects bead intent instead of incidental commit messages (Forge-l1x5)
- PR titles and descriptions now prefer English when available (Forge-aaxy)

## Other Changes

- Hearth Ready to Merge action menu now shows PR title (Forge-cqc6)