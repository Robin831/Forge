## Install

**Linux / macOS:**
```bash
curl -fsSL https://raw.githubusercontent.com/Robin831/Forge/main/install.sh | bash
```

**macOS (Homebrew):**
```bash
brew install Robin831/forge/forge
```

**Windows (PowerShell):**
```powershell
irm https://raw.githubusercontent.com/Robin831/Forge/main/install.ps1 | iex
```

**Windows (Scoop):**
```
scoop bucket add forge https://github.com/Robin831/scoop-forge
scoop install forge
```

Or download the archive for your platform below.

## Features

- Adventurer browser executor (Forge-i0fr)
- Batch CI fix and review fix modes for Copilot (Forge-99pk)
- Changelog fragment format validation (Forge-42fk)
- Combined Smith+Warden prompt for Copilot single-request mode (Forge-d4ey)
- Config file and disk space doctor checks (Forge-cxwi)
- Ingot lifecycle tracking in pipeline (Forge-y41p)
- Ingot status counts in Hearth dashboard (Forge-dw3n)
- Ingot status updates on PR lifecycle events (Forge-4eia)
- New `internal/ingot` package with schema, types, and CRUD operations (Forge-czem)
- New forge doctor checks for git, provider CLIs, and provider auth (Forge-9507)
- Per-anvil VCS provider support (Forge-cdp4)
- Quest CLI commands (Forge-9c3i)
- Quest types and YAML parsing for QuestGiver (Forge-oihe)
- QuestGiver configuration and daemon integration (Forge-91p4)
- QuestGiver monitor for E2E quest execution (Forge-ghtw)
- Skip Warden review for small Copilot diffs (Forge-mvqd)
- Staggered anvil poll timers (Forge-w5x0)
- Warden focused re-review on subsequent iterations (Forge-fwp7)
- `forge ingots list` and `forge ingots show` CLI commands (Forge-wfc8)
- `forge update` command for self-updating the binary (Forge-1xir)
- `warden_model_override` and `schematic_model_override` config settings (Forge-mtfa)
- forge doctor: Dolt connectivity, depcheck tooling, and changelog fragment checks (Forge-5rt6)

## Bug Fixes

- Allow all lifecycle actions on non-bead PRs (Forge-3l3w)
- Bellows no longer flags CI as failed while checks are still in progress (Forge-d793)
- Bellows no longer flags CI as failed while checks are still running (Forge-68vu)
- Depcheck npm scanner now syncs node_modules before scanning (Forge-cehs)
- Handle PR creation failures and duplicate PRs in finalizePipeline (Forge-yvu6)
- close bead immediately when PR merged via IPC (Forge-xxn4) (Forge-xxn4)
- depcheck aborts scan when git pull fails on anvil (Forge-5qce) (Forge-5qce)
- log dispatch failure to event log on every attempt (Forge-eyds) (Forge-eyds)
- run bead close in background after IPC merge to avoid timeout (Forge-kox3) (Forge-kox3)

## Other Changes

- Platform-aware `gh` check in `forge doctor` (Forge-khg5)
- Skip Schematic pre-analysis when primary provider is Copilot (Forge-qkn6)
- Update modernc.org/sqlite v1.46.1 â†’ v1.47.0 (Forge-neab)