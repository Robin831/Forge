## Install

**Linux / macOS:**
```bash
curl -fsSL https://raw.githubusercontent.com/Robin831/Forge/main/install.sh | bash
```

**macOS (Homebrew):**
```bash
brew install Robin831/forge/forge
```

**Windows (PowerShell):**
```powershell
irm https://raw.githubusercontent.com/Robin831/Forge/main/install.ps1 | iex
```

**Windows (Scoop):**
```
scoop bucket add forge https://github.com/Robin831/scoop-forge
scoop install forge
```

Or download the archive for your platform below.

## Features

- Anvil platform configuration (Forge-9lor)
- Auto-close decomposed parent beads with no dependents (Forge-qir6)
- Filterable event log in Hearth TUI (Forge-lm7w)
- GitHub VCS provider (Forge-1363)
- GitLab VCS provider (Forge-1363)
- Gitea/Forgejo VCS provider (Forge-ksia)
- Local model support via Ollama backend (Forge-d3l)
- OpenAI/GPT provider support (Forge-0m4)
- Per-anvil auto-merge for ready PRs (Forge-e827)
- Sequential dependencies between decomposed children (Forge-cxjw)
- Support 'no changes needed' as valid Smith outcome (Forge-33zf)
- Tool result correlation in Live Activity panel (Forge-6nrm)
- VCS provider interface (Forge-9lor)
- VCS provider interface (Forge-1363)
- Warden re-review, Approve as-is, and Force Smith actions for Needs Attention beads (Forge-wnlx)
- X-Forge-Event HTTP header on webhook notifications (Forge-9k1g)

## Bug Fixes

- Bellows-managed PRs now appear as workers in the Hearth Workers panel (Forge-p2y0)
- Close beads for PRs merged during daemon downtime (Forge-q0qp)
- Fix false empty-diff detection after smith pushes (Forge-z9h6)
- Force Smith no longer races with normal pipeline (Forge-1lqk)
- Force Smith note input now works correctly in Hearth (Forge-wnlx)
- Force smith now continues with temper/warden/PR instead of re-dispatching (Forge-nixc)
- Live Activity panel flows like a terminal (Forge-vk89)
- Orphan recovery no longer races with post-merge bead close (Forge-hp1h)
- Orphan recovery no longer resets beads parked for human attention (Forge-nbee)
- Prevent duplicate bellows workers in Hearth (Forge-uq7c)
- Prevent re-dispatch of decomposed parent beads (Forge-74a0)
- Prevent smith from misusing NO_CHANGES_NEEDED in warden review iterations (Forge-6gas)
- Rebase/cifix/burnish skip non-bead PRs (Forge-6sed)
- Schematic no longer exhausts turns without emitting verdict (Forge-tapr)
- Warden rules YAML quoting (Forge-dkty)
- reviewfix and cifix now run tests before pushing

## Other Changes

- All VCS consumers use the Provider interface (Forge-oxlb)
- Force Smith note pre-filled with warden rejection (Forge-q2tf)
- Improved text and thinking block rendering in Live Activity (Forge-dw67)
- Incremental log reading in Hearth (Forge-80ro)
- Live Activity panel now flows like Claude CLI terminal output (Forge-hy0b)
- Reduced smith re-exploration on warden/temper feedback iterations (Forge-zfok)
- Refactored GitHub PR operations behind VCS provider interface (Forge-co9n)
- Rich tool call formatting in Live Activity panel (Forge-3iim)