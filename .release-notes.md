## Install

**Linux / macOS:**
```bash
curl -fsSL https://raw.githubusercontent.com/Robin831/Forge/main/install.sh | bash
```

**macOS (Homebrew):**
```bash
brew install Robin831/forge/forge
```

**Windows (PowerShell):**
```powershell
irm https://raw.githubusercontent.com/Robin831/Forge/main/install.ps1 | iex
```

**Windows (Scoop):**
```
scoop bucket add forge https://github.com/Robin831/scoop-forge
scoop install forge
```

Or download the archive for your platform below.

## Features

- Context-sensitive keybinding hints in Hearth footer (Forge-ohpq)
- Docker images published to ghcr.io/robin831/forge (Forge-0nyl)
- Hearth: glamour-rendered bead descriptions (Forge-o0d4)
- Hot-reload notifications config without daemon restart (Forge-0ld8)
- Inline bead notes from the Hearth TUI (Forge-x7j0)
- Mouse support in Hearth TUI (Forge-ecue)
- Multiple generic webhook targets in notifications (Forge-9fpc)
- Release notifications via webhooks (Forge-284t)
- Toast notifications in Hearth TUI (Forge-xp95)
- Warden learns from CI fix patterns (Forge-fx7q)
- Warden validates diff against bead description to catch scope drift (Forge-95yi)
- `pr_ready_to_merge` webhook notification (Forge-2fzv)

## Bug Fixes

- Distinguish warden hard-reject from request-changes in event log (Forge-erdg)
- Fixed main branch hijacking by worktree feature branches (Forge-gll5)
- Hearth: press `m` to toggle mouse capture on/off (Forge-yt7c)
- Preserve smith logs after worktree cleanup (Forge-6153)

## Other Changes

- Hearth TUI now adapts colors for light and dark terminal backgrounds (Forge-l0a8)
- Hearth crucible panel uses bubbles progress bar (Forge-3hjr)
- Hearth log viewer now uses charmbracelet/bubbles viewport for scrolling (Forge-qian)
- Hearth now prompts before recovering orphan beads (Forge-zp4z)
- Rich webhook payloads with pre-formatted summary and structured metadata (Forge-si43)
- Use huh for action menus (Forge-dntc)