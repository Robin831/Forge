# Hearth 2.0 frontend bundle

This directory will hold the embedded React build output once Forge-4r9o
("Hearth 2.0 frontend MVP") lands. Until then the daemon serves the inline
HTML placeholder from `router.go` for the root path, and the frontend bead
will replace this file with `index.html`, `assets/...`, etc.
