# Beads Configuration File
# This file configures default behavior for all bd commands in this repository
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags

# Issue prefix for this repository (used by bd init)
issue-prefix: "Forge"

# Disable auto-start of a local Dolt server.
# Forge connects to a remote Dolt via kubectl port-forward (127.0.0.1:3306).
# Without this, beads auto-starts a local dolt when the port-forward drops,
# which conflicts and keeps restarting even after you kill it (idle-monitor watchdog).
dolt:
  auto-start: false

# Use no-db mode: load from JSONL, no SQLite, write back after each command
# When true, bd will use .beads/issues.jsonl as the source of truth
# instead of SQLite database
# no-db: false

# Disable daemon for RPC communication (forces direct database access)
# no-daemon: false

# Disable auto-flush of database to JSONL after mutations
# no-auto-flush: false

# Disable auto-import from JSONL when it's newer than database
# no-auto-import: false

# Enable JSON output by default
# json: false

# Default actor for audit trails (overridden by BD_ACTOR or --actor)
# actor: ""

# Path to database (overridden by BEADS_DB or --db)
# db: ""

# Auto-start daemon if not running (can also use BEADS_AUTO_START_DAEMON)
# auto-start-daemon: true

# Debounce interval for auto-flush (can also use BEADS_FLUSH_DEBOUNCE)
# flush-debounce: "5s"

# Git branch for beads commits (used by bd dolt push/pull for sync)
# IMPORTANT: Set this for team projects so all clones use the same sync branch.
# This setting persists across clones (unlike database config which is gitignored).
# Can also use BEADS_SYNC_BRANCH env var for local override.
# If not set, configure with: bd config set sync.branch <branch>.
# main is not protected here
# sync-branch: "beads-sync"

# Disable automatic JSONL backup - beads DB is on remote Dolt server (kubectl port-forward)
# so local JSONL backup is redundant (can also use BEADS_BACKUP_ENABLED)
backup:
  enabled: false
